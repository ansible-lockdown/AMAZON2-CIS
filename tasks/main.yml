---

- name: Check OS version and family
  ansible.builtin.fail:
      msg: "This role can only be run against Amazon Linux 2. {{ ansible_distribution }} {{ ansible_distribution_major_version }} is not supported."
  when:
      - ansible_distribution != 'Amazon'
      - ansible_distribution_major_version is version_compare('2', '!=')
  tags:
      - always

- name: Check ansible version
  ansible.builtin.fail:
      msg: You must use ansible 2.9 or greater
  when: not ansible_version.full is version_compare('2.9', '>=')
  tags:
      - always

- name: "check sugroup exists if used"
  block:
      - name: "Check su group exists if defined"
        ansible.builtin.command: grep -w "{{ amazon2cis_sugroup }}" /etc/group
        changed_when: false
        failed_when: sugroup_exists.rc >= 2
        register: sugroup_exists
        tags:
            - skip_ansible_lint

      - name: Check sugroup if defined exists before continuing
        ansible.builtin.assert:
            that: sugroup_exists.rc == 0
            msg: "The variable amazon2cis_sugroup is defined but does not exist please rectify"
  when:
      - amazon2cis_sugroup is defined
      - amazon2cis_rule_5_7
  tags:
      - rule_5.7

- name: Check pre-requisites
  ansible.builtin.include_tasks: check_prereqs.yml
  tags:
      - always
      - prereqs

- name: Preliminary tasks that should always be run
  ansible.builtin.include_tasks: prelim.yml
  tags:
      - prelim_tasks
      - always

- name: Pre remediation audit
  ansible.builtin.import_tasks: pre_remediation_audit.yml
  when:
      - run_audit
  tags:
      - run_audit

- name: Gather the package facts
  ansible.builtin.package_facts:
      manager: auto
  tags:
      - always

- name: Section 1
  ansible.builtin.include_tasks: section_1/main.yml
  when: amazon2cis_section1
  tags:
      - amazon2cis_section1

- name: Section 2
  ansible.builtin.include_tasks: section_2/main.yml
  when: amazon2cis_section2
  tags:
      - amazon2cis_section2

- name: Section 3
  ansible.builtin.include_tasks: section_3/main.yml
  when: amazon2cis_section3
  tags:
      - amazon2cis_section3

- name: Section 4
  ansible.builtin.include_tasks: section_4/main.yml
  when: amazon2cis_section4
  tags:
      - amazon2cis_section4

- name: Section 5
  ansible.builtin.include_tasks: section_5/main.yml
  when: amazon2cis_section5
  tags:
      - amazon2cis_section5

- name: Section 6
  ansible.builtin.include_tasks: section_6/main.yml
  when: amazon2cis_section6
  tags:
      - amazon2cis_section6

# - ansible.builtin.include_tasks: post.yml
#   tags:
#   - post_tasks
#   - always

- name: flush handlers
  ansible.builtin.meta: flush_handlers

- name: run post_remediation audit
  ansible.builtin.import_tasks: post_remediation_audit.yml
  when:
      - run_audit
  tags:
      - run_audit

- name: Show Audit Summary
  ansible.builtin.debug:
      msg: "{{ audit_results.split('\n') }}"
  when:
      - run_audit
  tags:
      - run_audit
